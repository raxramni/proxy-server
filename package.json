{
  "name": "proxy_server",
  "version": "1.0.0",
  "description": "Proxy Server assigment for NodeJs class",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --exec babel-node -- --optional strict --stage 1 -- index.js"
  },
  "author": "Rakesh Ramineni",
  "license": "ISC",
  "dependencies": {
    "request": "^2.61.0",
    "yargs": "^3.19.0"
  }
}
